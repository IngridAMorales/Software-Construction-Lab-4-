
name: CMake


on: 
   
   push: 
     branches: [ main, imora033/failing_tests, nwarn003/passing_tests ] 
   pull_request: 
     branches: [ main ] 

   workflow_dispatch:


jobs: 
    
  build: 

    runs-on: ubuntu-latest 


    steps: 
        
        - uses: actions/checkout@v2 

        - name: Running tests
          run: |
             cmake .
             make 
             ./test      
